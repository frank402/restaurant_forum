<!-- user_id：發出追蹤要求的 User，也就是目前登入的使用者，這個資訊會來自 current_user
following_id：使用者想要追蹤的人，這個資訊會來自在 @user.each 設定的變數 user -->

<ul>

  <% @users.each do |user| %>
    <li>
      <%= user.name %>
      <%= user.following_ids %>
      <%= link_to 'Follow', followships_path(following_id: user), method: :post %>
      <%= link_to 'Unfollow', followship_path(user), method: :delete %>
    </li>
  <% end %>

</ul>
